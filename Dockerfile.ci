FROM ghcr.io/astral-sh/uv:0.9.29-python3.12-bookworm-slim AS base

RUN apt-get update && apt-get install -y --no-install-recommends \
    make \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

COPY pyproject.toml uv.lock ./

RUN uv sync --all-extras
